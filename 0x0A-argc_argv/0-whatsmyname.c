#include <stdio.h>

/**
 * main - displays the name of the program
 * @argc: number of arguments passed to the function
 * @argv: commanad line argument strings pointer
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
